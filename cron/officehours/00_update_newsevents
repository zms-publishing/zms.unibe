#!/bin/bash

now=$(date)
echo $now cmsadm update NewsEvents
echo "==========================================================================" >> $VENV_HOME/log/api-cron.log
echo $now >> $VENV_HOME/log/api-cron.log
echo "--------------------------------------------------------------------------" >> $VENV_HOME/log/api-cron.log
$VENV_HOME/bin/python -m cmsapi.admin.main update NewsEvents &>> $VENV_HOME/log/api-cron.log