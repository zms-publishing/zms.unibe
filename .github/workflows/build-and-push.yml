name: Build and push Docker image
on:
  push:
    branches:
      - main
    tags:
      - "*"
jobs:
  call-build-and-push-workflow:
    uses: idasm-unibe-ch/reusable-workflows/.github/workflows/build-and-push.yml@1.0.0
