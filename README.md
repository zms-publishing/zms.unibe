# JS DataTables CMS
## How to use
Damit die Tabelle dargestellt werden kann, muss eine Datei mit dem Namen <code>(…)/data.json</code> erstellt werden.
Der Inhalt der Datei muss manuell von entsprechenden Webservice kopiert werden.