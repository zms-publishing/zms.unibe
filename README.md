# JS DataTables CMS
## How to use
Damit die Tabelle dargestellt werden kann, muss eine Datei mit dem Namen <code>test.json</code> erstellt werden.
Der Inhalt der Datei muss manuell von ParisCMS-WS kopiert werden.

Danach kann <code>index.html</code> im Browser geöffnet werden.